import { NgModule } from '@angular/core';
import { HttpClientModule } from '@angular/common/http';

// Definir servicios, módulos, dependencias centrales...


@NgModule({
  imports: [
    HttpClientModule
  ]
})
export class CoreModule { }
